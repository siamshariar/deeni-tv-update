# Disable optional dependencies to avoid sharp optionalInstall script failures in CI (Vercel pnpm)
optional=false
# Ensure pnpm version matches build environment expectation
package-manager=pnpm@10.28.0
