# Disable optional dependencies to avoid sharp optionalInstall script failures in CI
ignore-optional=true
# Also set npm-compatible omit flag so all installers skip optional deps
npm_config_omit=optional
# If Vercel sets package manager config, prefer pnpm 10.x (same as build log)
package-manager=pnpm@10.28.0
